// snapshot of: server-actions.client

const save = serverAction({ id: "oNNHTi6",
    callback: async (data: any) => {
        console.log(data);
    },
});

export default function Page() {
    return __tags.button({ __eid: 0, onClick: () => save({ text: "hi" }) }, ["Save"]);
}
