// snapshot of: fragments.client

export default function Page({ items, cond }: any) {
    return (
        [__tags.header(["Top"]), items.map((i: any) => (
                __tags.li({ key: i.id }, [i.name])
            )), cond ? __tags.span(["A"]) : __tags.span(["B"])]
    );
}
