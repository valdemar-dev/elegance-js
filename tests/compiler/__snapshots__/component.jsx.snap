// snapshot of: component.jsx

const Counter = component({
    atoms: {
        counter: 0,
    },
    view(_, { counter }) {
        return button({
            onClick: () => counter.value++,
        },
            `Counter: ${counter.value}`
        );
    }
});

export default function Page() {
    return Counter();
}
