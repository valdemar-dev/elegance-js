top level non-component elements can never be reactive, as the code containing them is not shipped to the browser. -- this is fine, but needs to be documented

client.ts is just fully slop. -- i want it to be like 2kb in prod.

server actions are technically enumerable -- idk if it's even a problem

patching rawhtml doesnt update dom -- feature ? 

all the caches are unbound rn -- fix!